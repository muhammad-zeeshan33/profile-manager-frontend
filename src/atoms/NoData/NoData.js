import React from 'react';

const NoData = () => {
    return (
        <div className="container text-center">
            <div className="jumbotron">
                <h2>No Data Available</h2>
                <p className="lead">There is no data to display.</p>
            </div>
        </div>
    );
};

export default NoData;
